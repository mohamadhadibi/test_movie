name: test_movie
description: Flutter test project for job employment.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # local language directions
  flutter_localizations:
    sdk: flutter
  # Service locator
  get_it: ^8.2.0
  # Bloc for state management
  flutter_bloc: ^9.1.1
  bloc: ^9.0.0
  # Value equality
  equatable: ^2.0.7
  # Functional programming thingies
  dartz: ^0.10.1
  # Local cache
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cached_network_image: ^3.3.1
  # list view
  infinite_scroll_pagination: ^4.0.0
  # movie sdk
  tmdb_api: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  bloc_test: ^10.0.0
  gen_lang:
    git:
      url: https://github.com/KingWu/gen_lang
      ref: 7ba7548
  flutter_lints: ^6.0.0
  lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
